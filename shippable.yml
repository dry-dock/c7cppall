language: cpp

compiler:
  - gcc #8.0.2
  - clang #5.0.1

build:
  pre_ci:
    - docker build -t=drydock/c7cppall:dev .
  pre_ci_boot:
    image_name: drydock/c7cppall
    image_tag: dev
    pull: false
  ci:
    - ls
    - pwd
    - java --version
    - node -v
    - python --version
